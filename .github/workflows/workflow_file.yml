name: Workflow Name

on:
  push:
    branches:
      - main

jobs:
  job_name:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Step where gh is used
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # rest of the commands
